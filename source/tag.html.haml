---
pageable: true
per_page: 12
---

.archive
  %h1.archive__title
    「 #{tagname} 」のtagが付いた記事
  - if paginate && num_pages > 1
    %p
      Page #{page_number} of #{num_pages}
    - if prev_page
      %p= link_to 'Previous page', prev_page
  %ul.archive__itemes
    - page_articles.each_with_index do |article, i|
      %li.archive__iteme
        %time.archive__article-date= article.date.strftime('%Y/%m/%d')
        %p.archive__article-title= link_to(article.title, current_article, class: "sub-blog-entry__link")

  - if paginate
    - if next_page
      %p= link_to 'Next page', next_page
