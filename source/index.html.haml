---
pageable: true
per_page: 10
---

- if paginate && num_pages > 1
  %p
    Page #{page_number} of #{num_pages}
  - if prev_page
    %p= link_to 'Previous page', prev_page

- page_articles.each_with_index do |article, i|
  %article.blog-article
    %time.blog-article__time= article.date.strftime('%Y/%m/%d')
    %h2.blog-article__title
      = link_to(article.title, article, class: "blog-article__link")
    .blog-article__article.blog-article-content= article.summary(250)
    %footer.blog-article__footer= link_to 'Read More', article.url, class: "blog-article__read-more is-lobster"

- if paginate
  - if next_page
    %p= link_to 'Next page', next_page
